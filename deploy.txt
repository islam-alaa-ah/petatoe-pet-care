P51146 Deployment

1) Merge files preserving paths.
2) Run Supabase SQL: supabase/migrations/p51146.sql
3) Hard refresh/update PWA to 18.54.28.

Acceptance:
- Start an appointment to any execution stage.
- As Super Admin/Sales Manager press return-to-scheduling and enter a mandatory reason.
- Request returns to scheduling queue and can be assigned again.
- Old execution visit remains in DB with status ملغاة.
- Open تقارير المواعيد > ملخص المواعيد for that day.
- سجل إعادة الجدولة اليومي must show request number, stage, reason, previous team/groomer/driver and who performed it.
- Groomer/Driver still cannot see or execute the return button/RPC.
