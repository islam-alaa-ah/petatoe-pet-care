P51141 Deployment

1) Merge delivered frontend files preserving paths (safe even if P5114 frontend was already merged).
2) Run:
   supabase/migrations/p51141.sql
3) Run/inspect:
   supabase/verification/verify.sql

Expected:
- get_installation_schedule_global exists
- return_type = jsonb
- authenticated_can_execute = true
- schedule_global_returns_jsonb = true
- schedule_save_exists = true
- create_appointment_exists = true

Then test:
Create Appointment -> Save -> automatic Scheduling screen -> appointment visible ->
assign team + slot -> Groomer/Driver sees only own team's appointment -> Execution.
