(function(){
  'use strict';
  const db=()=>{if(!window.customerSupabase)throw new Error('اتصال Supabase غير جاهز.');return window.customerSupabase};
  async function listScreen(screenKey){const{data,error}=await db().from('app_translations').select('translation_key,screen_key,text_type,ar_text,en_text').eq('screen_key',screenKey).eq('is_active',true).order('translation_key');if(error)throw new Error(`تعذر تحميل الترجمات: ${error.message}`);return data||[]}
  async function listPilot(){const{data,error}=await db().from('app_translations').select('translation_key,screen_key,text_type,ar_text,en_text').in('screen_key',['installationExecution','sidebar','shared','installationExecutionServices','installationExecutionNeighborhoods']).eq('is_active',true).order('screen_key').order('translation_key');if(error)throw new Error(`تعذر تحميل ترجمات المرحلة التجريبية: ${error.message}`);return data||[]}
  async function listEntityCatalog(){const[services,neighborhoods]=await Promise.all([db().from('installation_service_types').select('id,name,service_code').order('name'),db().from('installation_neighborhoods').select('id,name').eq('is_active',true).order('name')]);if(services.error)throw new Error(`تعذر تحميل الخدمات لمركز الترجمه: ${services.error.message}`);if(neighborhoods.error)throw new Error(`تعذر تحميل الأحياء لمركز الترجمه: ${neighborhoods.error.message}`);return{services:(services.data||[]).map(x=>({id:x.id,name:x.name||'',serviceCode:x.service_code||''})),neighborhoods:(neighborhoods.data||[]).map(x=>({id:x.id,name:x.name||''}))}}
  async function saveEntries(entries){if(!window.CustomerPermissions?.requireAction?.('translationCenter','edit',{silent:true}))throw new Error('ليس لديك صلاحية تعديل مركز الترجمه.');const uid=window.CustomerAuth?.getState?.().profile?.id||null,payload=(entries||[]).map(x=>({translation_key:x.translation_key,screen_key:x.screen_key||'installationExecution',module_name:x.module_name||'appointments',text_type:x.text_type||'label',ar_text:String(x.ar_text||'').trim(),en_text:String(x.en_text||'').trim(),default_ar:String(x.default_ar||'').trim(),default_en:String(x.default_en||'').trim(),is_active:true,updated_by:uid,updated_at:new Date().toISOString()}));const{error}=await db().from('app_translations').upsert(payload,{onConflict:'translation_key'});if(error)throw new Error(`تعذر حفظ مركز الترجمه: ${error.message}`);return payload}
  async function saveScreen(screenKey,entries){await saveEntries((entries||[]).map(x=>({...x,screen_key:x.screen_key||screenKey})));return listScreen(screenKey)}
  window.LocalizationCenterService=Object.freeze({listScreen,listPilot,listEntityCatalog,saveEntries,saveScreen});
})();
